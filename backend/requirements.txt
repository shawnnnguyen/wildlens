# ── API layer (backend/api) ────────────────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.52.0
python-multipart==0.0.32

# ── Orchestration ──────────────────────────────────────────────────────────────
langgraph==1.2.10
langgraph-checkpoint-sqlite==3.1.1   # SqliteSaver — checkpointer used by the backend in production

# ── Retry/backoff ──────────────────────────────────────────────────────────────
tenacity==9.1.4

# ── LangChain — 1.x era (langchain-core 1.x / langchain 1.x) ─────────────────
langchain==1.3.14
langchain-core==1.5.3
langchain-openai==1.4.1              # DeepSeek text LLM (OpenAI-compatible API)
langchain-google-genai==4.3.2        # Gemini for multimodal vision
langchain-community==0.4.2           # FAISS integration + BM25Retriever
langchain-huggingface==1.2.2         # HuggingFace embeddings

# ── Vector store — Pinecone (cloud) ────────────────────────────────────────────
langchain-pinecone==0.2.13           # LangChain integration for Pinecone
pinecone==7.3.0                      # Pinecone Python client v3+

# ── Web search — Tavily (live retrieval fallback/supplement) ──────────────────
tavily-python==0.7.27

# ── Document + image store — Supabase ─────────────────────────────────────────
supabase==2.31.0                     # Supabase Python client

# ── Observability ───────────────────────────────────────────────────────────────
langfuse==4.14.2                     # OTEL-based SDK: global Langfuse() client + CallbackHandler (optional).
                                      # eval/run_eval.py's dataset.run_experiment()/client.run_experiment()
                                      # usage was verified against this exact installed version.

# ── BM25 keyword retriever ─────────────────────────────────────────────────────
rank-bm25==0.2.2                     # BM25 keyword retriever for hybrid search

# ── Retrieval-result caching (opt-in — see _EnsembleRetriever.cache_dir) ───────
diskcache==5.6.3                     # on-disk cache, preferred over Redis at this scale

# ── Embeddings ─────────────────────────────────────────────────────────────────
sentence-transformers==5.6.1

# ── Structured output ──────────────────────────────────────────────────────────
pydantic==2.13.4

# ── TTS (install at least one) ─────────────────────────────────────────────────
edge-tts==7.2.8                      # preferred: Microsoft Neural voices, free, high quality
# gTTS>=2.3.2                        # fallback: Google TTS, simpler, requires internet

# ── Data ingestion ─────────────────────────────────────────────────────────────
httpx==0.28.1                        # sync HTTP client for EOL + IUCN API calls
ijson==3.5.1                         # streaming JSON parser for LILA BC ~500 MB metadata

# ── Environment management ─────────────────────────────────────────────────────
python-dotenv==1.2.2
