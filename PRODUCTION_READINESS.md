# Wildlens — Production Readiness Roadmap

Living tracker for hardening `wildlens` (LangGraph agent in `agent/src/wildlens/` + FastAPI backend in `backend/`) toward production. Originated from an architecture review against four pillars: Architecture & Orchestration, Reliability & Guardrails, Cost & Latency Optimization, Observability & Evaluation. Update this file as items land — check them off, don't delete the history.

**Product context that shapes Phase 1 below:** this is a single-use, accountless app — tourists snap a photo, chat, and leave; nobody expects the session to survive after they close the tab. That rules out a login/user-account model as the fix for session security, and it means the eval/observability gap should build on the Langfuse integration already in place rather than a parallel bespoke harness. See the revised Phase 1 auth item and Phase 3 eval item below.

**Last updated:** 2026-07-09 (Phase 3: golden-dataset eval + structured JSON logging landed, on branch `feat/implement-observability-and-eval`, not yet merged)

## Executive Summary

Wildlens is a well-architected **prototype**: the LangGraph state machine is clean, reducers are used correctly, the RAG ensemble is genuinely hybrid (BM25 + Pinecone + Tavily with RRF fusion), and Langfuse tracing is *actually wired in* (not just documented) with custom image redaction. This is above-average engineering discipline for a project at this stage.

It is **not yet production-hardened**. Highest-priority risks, in order:

1. **Unbounded/leaking memory** — `identification_history` grows forever per session; `DELETE /api/sessions/{id}` doesn't actually clear the in-memory checkpointer, only a tracking set. Combined with `MemorySaver` (pure in-process RAM, no persistence), this is a slow memory leak and a full data-loss-on-restart risk.
2. **No per-session secret** — `thread_id` is a client-generated UUID (`crypto.randomUUID()`, `frontend/src/hooks/useSessions.ts:8,56`) that doubles as the *only* credential for chat continuation, `GET /history`, and `DELETE`. Anyone who obtains the ID (browser history, a shared screen, a proxy log) can read or evict someone else's session. This is not a missing-auth-model problem — the app is intentionally accountless and single-use — it's a missing capability token, a much smaller fix.
3. **Silent failure modes** — two of the three LLM-calling nodes have zero error handling; the RAG ensemble swallows sub-retriever failures into an empty result set with no alerting. No retry/backoff anywhere in the codebase.
4. **No accuracy evaluation** — for an app whose entire value proposition is "correctly identify this animal," there is no golden dataset, no LLM-as-judge, no regression tracking.
5. **No cost controls** — no semantic caching, no `max_tokens` caps, single model tier for cheap and expensive tasks alike.

None of this requires a rebuild — the graph topology, state schema, and RAG layer are solid foundations. The gaps are additive (retry decorators, an auth check, a caching layer, an eval harness), not structural.

---

## Pillar 1: Architecture & Orchestration

**Current state**
- Single fixed `StateGraph` (`agent/src/wildlens/graphs.py`), compiled once with `MemorySaver()` (line 235 at time of writing). Routing is three binary conditional edges (`route_entry`, `route_after_analysis`, `route_audio`) plus, as of PR #9, a topic-relevance gate — see Phase 2 below.
- `node_generate_guide_persona` (`nodes.py:601-704`) is a monolithic call: persona narration + fact reconciliation (guidebook vs. web) + cross-session digest + intro-vs-follow-up branching, all in one `llm.invoke()`.
- `chat_history` summarization is well-implemented: sliding 6-message raw window, delta-only LLM calls beyond `SUMMARY_THRESHOLD=10` (`nodes.py:538-594`).
- `identification_history` (`state.py:66`, `operator.add` reducer) is **unbounded**.
- No cross-session/long-term vector memory; `MemorySaver` is pure in-process RAM (no persistence across restarts, no horizontal scaling).

---

## Pillar 2: Reliability & Guardrails

**Current state**
- `node_analyze_image` has a graceful try/except fallback; `node_summarize_history` and `node_generate_guide_persona` have **no error handling** — failures bubble to a generic 500.
- No retry/backoff library anywhere (no `tenacity`, no manual backoff) for LLM calls, Pinecone, Supabase, or ingestion HTTP clients.
- TTS fallback chain (`tts.py`) only covers *import-time* unavailability, not *runtime* failures — a network drop mid-synthesis raises uncaught instead of falling through edge-tts → gTTS.
- No fallback LLM provider for Gemini (vision) or DeepSeek (text) — single point of failure per modality.
- Supabase runtime reads use the same service-role key as ingestion writes — no read-only credential split.
- `DELETE /api/sessions/{thread_id}` (and `GET /history`, and chat continuation) trust a client-generated `thread_id` as if it were a secret — nothing is checked against it, so knowing/guessing a `thread_id` is sufficient to read or evict someone else's session. `DELETE` also doesn't actually clear the `MemorySaver` checkpoint (only a tracking set) — unauthorized *and* incomplete.
- No human-in-the-loop gate anywhere in the graph.
- Global exception handler does *not* leak stack traces to clients — this part is solid.

---

## Pillar 3: Cost & Latency Optimization

**Current state**
- Single model tier per modality: `gemini-2.5-flash` for vision (`backend/main.py:64`, `GEMINI_VISION_MODEL`), `deepseek-chat` for all text generation (summarization *and* persona generation reuse the same instance, no cheaper tier for the lighter task). No `max_tokens` cap set anywhere.
- No semantic/response caching anywhere in the runtime path (repo-wide grep for redis/diskcache/gptcache/lru_cache returns nothing in production code).
- ~~Retrieval fired unconditionally on every text turn, including small talk~~ — **addressed by PR #9**, see Phase 2.

---

## Pillar 4: Observability & Evaluation

**Current state**
- Real Langfuse integration (`agent/src/wildlens/observability.py`) — custom image redaction (base64 data URIs stripped, long strings truncated; no actual PII/name/email scrubbing despite the "PII redaction" label this used to carry in this doc), per-turn spans, manual `@observe()` wrapping for plain-Python nodes, and per-turn metadata (species/confidence/threat_level) already attached to every trace (`observability.py:96-104`). Genuinely solid, not just documented env vars. Token usage/cost per generation is also already captured natively by Langfuse's `CallbackHandler` — no extra aggregation layer needed, just query it.
- Structured logging is plain-text, not JSON; no OpenTelemetry/Datadog/LangSmith.
- Test suite (`agent/tests/`) is unit tests with fully mocked LLMs/retrievers — good for code-logic regressions, zero coverage for identification accuracy or persona-quality regressions.
- **No golden dataset, no LLM-as-judge, no accuracy regression tracking** — for an ID-accuracy product, this is the single biggest blind spot. Langfuse already has the primitives to close it (Datasets, Scores, Evaluators) — this doesn't need new infra, see Phase 3.

---

## Phased Roadmap

### Phase 1 — Critical (stop the bleeding: leaks, crashes, security)

- [x] **Per-session capability token** (branch `phase1/production-hardening`) — `backend/session_registry.py` (rewritten: SQLite-backed, atomic `create()` INSERT so "is this a brand-new thread_id" and "register it" are one DB op, no check-then-set race), `backend/routers/chat.py`, `backend/routers/sessions.py`. First `/chat` call for a new `thread_id` gets a secret back in `ChatResponse.session_secret`; every later call (continuation, `GET /history`, `DELETE`) must present it via `X-Session-Secret`, checked with `hmac.compare_digest`. Frontend (`useSessions.ts`, `client.ts`) stores and resends it. `GET /api/audio/{filename}` intentionally left ungated (unguessable temp filename, non-secret content). 9 integration tests in `backend/tests/test_session_auth.py` cover creation/rejection/cross-session-isolation/history/delete/retry-after-validation-failure.
  - Caught by an independent no-context review pass and fixed before merge: (1) `backend/main.py`'s CORS `allow_headers` didn't list `X-Session-Secret`, so the browser preflight would have silently blocked every follow-up request cross-origin (the default dev setup) — added. (2) `registry.create()` ran before input validation/graph invocation, so a first request that failed (bad image, graph error) orphaned the session — the client never got the secret, but the thread_id was already permanently consumed. Fixed by wrapping the turn in `try/except HTTPException` and `registry.evict()`-ing on any failure for a newly-created session, so a retry with the same thread_id is treated as a fresh first call.
- [x] **SqliteSaver checkpointer** — `agent/src/wildlens/graphs.py` (`build_graph` now takes an injectable `checkpointer`, defaults to `MemorySaver` for tests/local), wired up in `backend/main.py` (same SQLite file as the session-secret table, `SESSIONS_DB_PATH` env var). `DELETE /api/sessions/{id}` now calls `graph.checkpointer.delete_thread()` for real.
- [x] **Bounded `identification_history`** — `agent/src/wildlens/state.py` (`_bounded_identification_history` reducer, `MAX_IDENTIFICATION_HISTORY=15`).
- [x] **Tenacity retry** around LLM calls in `node_summarize_history` / `node_generate_guide_persona` — `agent/src/wildlens/nodes.py` (`_invoke_with_retry`, 3 attempts/exponential backoff). On exhausted retries: summarize degrades to a no-op (self-heals next call), persona degrades to a fixed apology script + surfaced `error_message` (never breaks the "final_script always set" contract). Covered by `test_summarize_history_llm_failure_retries_then_noop` / `test_persona_llm_failure_retries_then_returns_apology_script`.
- [x] **TTS runtime-failure fallthrough** — `agent/src/wildlens/tts.py` (`synthesise_audio`: edge-tts/gTTS calls now wrapped so a runtime failure falls through the chain instead of raising uncaught; both-failed still returns the `"NO_TTS_ENGINE_INSTALLED"` sentinel chat.py already handles gracefully). `agent/tests/test_tts.py`.
- [x] **Retry + escalated logging around RAG sub-retrievers** — `agent/src/wildlens/rag/ranking.py` (`_call_with_retry`, 1 retry per sub-retriever call; exhausted-retry failures escalated from `log.warning` to `log.error` — true paging/alerting is Phase 3 structured-observability scope, no such channel exists yet). `test_failing_retriever_retries_then_degrades_to_empty_not_raise` in `test_rag.py`.
- [x] **Supabase credential split** — `agent/src/wildlens/data/supabase_store.py` (`SupabaseStore(role=...)`, `SUPABASE_INGEST_KEY` vs `SUPABASE_RUNTIME_KEY`, falls back to `SUPABASE_KEY` if unset). Note: the doc's original "anon/read-only key at runtime" framing turned out slightly wrong — `ranking.py`'s enrichment write-back (`_write_enrichment`) means the runtime process legitimately needs `INSERT`/`UPDATE` on `documents`, not pure read-only; the split is least-privilege (species/documents read + documents write) rather than strictly read-only. RLS policy sketch in the class docstring; actual Supabase role/policy setup is dashboard config outside this repo.

### Phase 2 — Optimization (cost, latency, quality)

- [x] **Intent-gated retrieval** — `node_check_relevance` (merged `6d131ed`, PR #9): classifies text turns `on_topic`/`small_talk`/`off_topic` via cheap heuristics before an LLM fallback; skips retrieval for small talk; targets retrieval at the mentioned species instead of always the last-identified one.
  - Follow-up gaps found in a Fable review pass of the merged implementation, re-verified against current `HEAD` by a Plan-agent review pass:
    - [x] Generic-noun alias false positives — fixed in `b2e12ec`: `agent/src/wildlens/data/species_lookup.py:30` (`_GENERIC_HEAD_NOUNS` now includes `"roller"`, `"monkey"`); "roller coaster"/"monkey business" no longer misroute to a specific species. Regression tests in `agent/tests/test_species_lookup.py`.
    - [x] Small-talk phrase match swallowing real questions — fixed in `b2e12ec`: `_is_small_talk` (`nodes.py:296-311`) now requires the *entire* message (modulo filler words) to equal a known phrase rather than a contains-anywhere match, so "hi, where is my mom?"/"hey, does it bite?" fall through to the embedding/LLM tiers instead of being swallowed. `node_check_relevance` (`nodes.py:496-562`) also deliberately runs species-mention and keyword checks *before* the small-talk check for the same reason. Regression tests in `agent/tests/test_nodes.py`.
    - [x] **(Priority)** LLM relevance fallback had zero conversation context — fixed in `b2e12ec`: `_llm_classify_relevance` (`nodes.py:340-375`) now takes `session_species` (threaded from `node_check_relevance:560`) and injects it via `_RELEVANCE_CONTEXT_LINE` (`nodes.py:332-337`), so pronoun/contextual follow-ups like "can it swim?" are classified with awareness of recently-discussed species instead of being evaluated in isolation.
    - [ ] Multi-animal messages ("lion vs elephant, who wins?") only target one species for retrieval — acceptable known limitation, document rather than fix unless it becomes a real complaint.
    - [x] Add regression tests for the pronoun-follow-up and small-talk-swallow cases in `agent/tests/test_nodes.py` — added in `b2e12ec` (pronoun-follow-up-with-context, greeting-plus-real-question, bare-pleasantry-not-swallowed, small-talk-phrase-plus-content rejection) plus one more added afterward covering a non-exemplar wildlife phrasing ("will it hurt me") through the full gate, to confirm it resolves via the embedding tier rather than being silently mishandled upstream.
  - **New in `b2e12ec`, beyond what this list originally anticipated:** an embedding-based relevance tier (`nodes.py:378-493`) now sits between the small-talk check and the LLM fallback — embeds the message and compares it via cosine similarity against curated on-topic/off-topic exemplar clusters (using the retriever's existing local HuggingFace embedding model, wired through `graphs.py`), resolving common phrasings (e.g. "does it bite", "will it hurt me") without an LLM call. `node_check_relevance` is now a 5-stage cascade (species mention → keyword → small talk → embedding similarity → LLM), not 4.
- [x] **`max_tokens` caps** — `backend/main.py`: `llm_vision` (`ChatGoogleGenerativeAI`) gets `max_output_tokens` (env `GEMINI_VISION_MAX_TOKENS`, default 2048) **and** `thinking_budget=0` (env `GEMINI_VISION_THINKING_BUDGET`) — confirmed via the installed `langchain-google-genai==4.2.5` fields that Gemini 2.5's thinking tokens share the same output-token budget as the structured-output JSON, so disabling thinking for this pure-extraction call is a cleaner fix than just raising the cap (a Fable review pass caught that the originally-planned 1024-token cap would have made truncation *more* likely, not less). `llm_text` (`ChatOpenAI`/DeepSeek) gets `max_tokens` (env `DEEPSEEK_MAX_TOKENS`, default 2048). The previously-planned "mock a parse exception" regression test was dropped — review found it would only re-exercise the existing generic-exception test with no real truncation coverage; actual validation is the manual real-call check in Verification below.
- [ ] Model tiering for cheap text tasks — **deferred**, not silently dropped: DeepSeek has no cheaper sibling in use today, and introducing a second LLM provider (e.g. reusing Gemini for `node_summarize_history`/`_llm_classify_relevance`) is a bigger decision than this phase warrants without cost data justifying it yet. Revisit once Langfuse `queryMetrics` shows DeepSeek text-generation cost is actually worth optimizing.
- [x] **Retrieval-result caching** — `agent/src/wildlens/rag/ranking.py`: `_EnsembleRetriever` gains an opt-in `cache_dir`/`cache_ttl_seconds` (default `None`/6h — every existing test and default `init_rag()` call is unaffected) diskcache-backed cache around `retrieve()`. Cache key is `(normalized_query, species, corpus_version, cross_encoder_ready)` — the latter two dimensions were added after an independent review pass found the original per-BM25-rebuild-only version counter left a staleness gap (Pinecone's `web_cache` namespace updates immediately per enrichment write, `enrichment_rebuild_every=10` writes later for BM25), and that caching during the cross-encoder's async background-load window (`factory.py`'s `_load_cross_encoder_async`) would pin unreranked results for the full TTL. `_corpus_version` now bumps once per successful `_write_enrichment` call, not just on rebuild. Empty/degraded (`[]`) results are never cached. `cache_dir` threaded through `factory.py`'s `init_rag()` and `backend/main.py` (env `RETRIEVAL_CACHE_DIR`, `RETRIEVAL_CACHE_TTL_SECONDS`). New dependency: `diskcache>=5.6.0`. Tests in `agent/tests/test_rag.py`: `test_retrieval_cache_hit_avoids_second_sub_retriever_call`, `test_retrieval_cache_invalidated_by_enrichment_write`, `test_retrieval_cache_does_not_cache_empty_results`, `test_retrieval_cache_disabled_by_default`.
- [x] **Decompose the persona node (mechanical option)** — `agent/src/wildlens/nodes.py`: the intro-vs-follow-up branch in `node_generate_guide_persona` (`nodes.py:872` at time of writing) now picks between two named module-level templates (`_PERSONA_FOLLOWUP_TASK_TEMPLATE`, `_PERSONA_INTRO_TASK_TEMPLATE`, defined near `_BAAKO_SYSTEM`) and fills them via `.format()`, instead of building two inline f-strings — no behavior or cost change, no new LLM call (fact-reconciliation as a separate call remains deferred to Phase 3+, per the original scope decision). A review pass found the follow-up branch had **zero** test coverage before this change (every existing persona test used the default empty `user_message`), so three characterization tests were added first, before the extraction, to lock behavior: `test_persona_followup_turn_interpolates_question_and_instructs_baako`, `test_persona_followup_turn_includes_animals_digest_when_multiple_species_seen`, `test_persona_followup_turn_omits_animals_digest_with_single_history_entry` (the digest only renders for `len(identification_history) > 1` — a single-entry history must not show it). All 5 pre-existing intro-branch tests (`nodes.py` lines 719, 751, 768, 788, 859 in `test_nodes.py`) still pass unchanged.

### Phase 3 — Scale (multi-provider resilience, observability, eval)

- [ ] LLM provider failover — `.with_fallbacks()` for `llm_vision`/`llm_text` secondary providers (`backend/main.py`)
- [ ] Postgres checkpointer — swap `SqliteSaver` for `PostgresSaver` only once multi-instance/concurrent deployment is actually needed
- [ ] HITL gate for low-confidence IDs — `interrupt()` tied into the existing `route_after_analysis` confidence gate
- [x] **Golden-dataset eval via Langfuse Datasets** — `agent/src/wildlens/eval/`: `seed_dataset.py` builds dataset `wildlens-golden-v1` from images already ingested into the `wildlife-images` Supabase bucket (`image_files` table joined to `species_list.json` ground truth — a new `SupabaseStore.list_image_files()` read method; storage-path idempotent item ids so re-seeding upserts rather than duplicates), `run_eval.py` replays it through the compiled graph via the real langfuse Python SDK API — `dataset.items` / `client.run_experiment(data=..., task=..., evaluators=..., run_evaluators=...)` (present since `4.0.0`, verified directly against both the installed `4.12.0` and a downloaded `4.0.0`), not the `upsertDataset`/`item.run()` shape this item originally described, which doesn't exist in the installed SDK (caught by an independent no-context Fable review pass of the plan before implementation started, and re-verified by a second no-context Fable review of the finished implementation). `scorers.py` — `species_match`/`threat_level_match` (both distinguish `abstained`, from the `MIN_CONFIDENCE` gate, from `wrong`, a confident misidentification — collapsing the two would hide the difference this eval exists to surface), `confidence_calibration` + a `mean_confidence_when_wrong` run-level aggregate, and `persona_hallucination` (Gemini-as-judge — not DeepSeek judging its own persona output — skipped entirely on abstained turns, where the canned retry script has nothing to hallucinate about). `max_concurrency` explicitly capped (SDK default is 50, which would hammer Gemini/DeepSeek rate limits). A second review pass (post-implementation) found the abstained/wrong split didn't account for `node_analyze_image` exceptions — `node_unclear_photo_fallback` overwrites `error_message` to the same generic sentinel regardless of cause, so a Gemini API outage on a golden-set item was indistinguishable from an honest low-confidence read; fixed with a third `"error"` category (`_is_analysis_exception`'s stub-shape heuristic in `scorers.py`) plus `run_eval.py`'s `task()` now catching its own image-download/graph-invocation failures instead of letting them silently drop the item from `item_results` with no score at all. Not run per-commit: `.github/workflows/eval.yml` (`workflow_dispatch` + weekly `schedule`). The roadmap's online-evaluator-on-live-traffic idea has no Python SDK surface as of 4.12 — documented as a Langfuse-UI-only config step in `run_eval.py`'s module docstring rather than silently dropped. Tests in `agent/tests/test_eval.py` (scorers + task function, fully mocked, no real Langfuse/LLM calls, matching this repo's test convention).
- [x] **Structured observability** — `agent/src/wildlens/logging_config.py`: stdlib-only JSON `logging.Formatter` (no new dependency), wired into both entrypoints — `backend/main.py` (replacing its previously-nonexistent root config, which meant every `log.info` in the backend was silently dropped by the WARNING lastResort handler) and `agent/src/wildlens/__main__.py` (replacing its existing plain-text `basicConfig`). `thread_id` is attached to every log line via a `ContextVar` set in `backend/routers/chat.py` and a `logging.Filter` — propagates into the graph-invocation worker thread for free via `asyncio.to_thread`'s context-copying, and is already the same value Langfuse uses as `langfuse_session_id`, so logs and traces are joinable on one field. uvicorn's own access/error loggers (`propagate=False`, invisible to a root-handler swap) are routed through the same formatter via `uvicorn_log_config()`, used by a new `python -m backend.main` entrypoint (`uvicorn backend.main:app` still works, just without JSON uvicorn access logs). No separate token-usage aggregation added — Langfuse's `CallbackHandler` already captures per-generation cost/usage natively; query it via `queryMetrics`. Tests in `agent/tests/test_logging_config.py`.

---

## Verification per phase

- **Phase 1**: done — `pytest agent/tests/ backend/tests/` (110 passing: reducer/retry/TTS/RAG-retry fallback behavior in `agent/tests/`, capability-token creation/rejection/cross-session-isolation/history/delete in `backend/tests/test_session_auth.py`, no external API keys required for either).
- **Phase 2**: manual latency comparison (small-talk turn before/after intent gating — already landed). For the newly-landed items: (1) `max_tokens` caps — still need one real Gemini structured-output call and one real DeepSeek persona-script call to sanity-check `GEMINI_VISION_MAX_TOKENS`/`DEEPSEEK_MAX_TOKENS` don't truncate legitimate output (unit tests mock the LLM, so this can't be verified by `pytest` alone). (2) Retrieval caching — target cache hit rate > 30% on repeated-species-question sessions once `RETRIEVAL_CACHE_DIR` is set in a real deployment (tempered expectation: this app is single-use/accountless, so most benefit is likely within-session repeats, not cross-session); verify via a repeated-query session trace showing no Tavily/BM25/Pinecone calls logged on the second identical query. Prefer pulling before/after cost & latency numbers via Langfuse `queryMetrics` (token usage/cost is already captured natively per-generation, per Pillar 4) over one-off manual checks, since manual checks aren't repeatable evidence.
- **Phase 3**: `pytest agent/tests/test_eval.py agent/tests/test_logging_config.py` covers scorer/formatter logic with no external calls. Still needed before calling this done in practice: (1) `python -m wildlens.eval.seed_dataset` then `python -m wildlens.eval.run_eval --limit 5` against a real environment, confirming a run appears in the Langfuse UI under `wildlens-golden-v1` with all four scores attached and a low-confidence test image scoring `abstained` rather than `wrong`; re-run the seed script once more to confirm no duplicate items. (2) Start the backend via `python -m backend.main`, confirm stdout is JSON-per-line including uvicorn's own lines, and that `thread_id` appears on request-scoped log lines. Re-run the golden-dataset run before/after a prompt or model change to confirm scores move as expected.
